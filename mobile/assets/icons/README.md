# Icons

| File | Size / aspect | Purpose |
|------|---------------|---------|
| `compass.svg` | 64×64 viewBox | Center glyph for `CompassDivider`. Use `fill="currentColor"` so `ColorFilter.srcIn` in the widget can re-tint it. |

Monochrome only. Add new SVGs here the same way (currentColor fills).
