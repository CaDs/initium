# Login hero — PLACEHOLDER

| File | Size / aspect | Purpose |
|------|---------------|---------|
| `hero.svg` | 1080×1200 | Top-third arch-clipped hero behind the login card |

Rendered with `GradientContainer(blendMode: colorBurn)` overlay to warm-tint the image.
Swap with an editorial photo / illustration sized 9:10 or wider. No code changes.
